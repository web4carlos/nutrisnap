[project]
name = "nutrisnap-backend"
version = "0.1.0"
description = "NutriSnap AI-powered nutrition platform backend"
requires-python = ">=3.13"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py313"
